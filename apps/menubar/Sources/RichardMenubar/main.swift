// Keep this file TRULY empty to ensure there is no top-level code in the module.
// Previously comments here still let Xcode compile the file as a source unit and can confuse diagnostics
// when combined with SwiftPM/Xcode project graph mixing. Removing all content guarantees no entry point here.
